1. Подкласс должен сохранять поведение базового класса, чтобы его можно было безопасно заменить.


2. Design by Contract (DBC) — это идея, что каждый метод должен чётко определять:

✅ Предусловия (что должно быть верно до вызова) - preconditions (Условия, которые должны быть выполнены перед вызовом функции, обязанность вызывающего (клиента))

✅ Постусловия (что должно быть верно после вызова) - postcontidions (Условия, которые должны быть выполнены после выполнения функции, если предусловия были соблюдены. Это обязанность функции.)

✅ (Опционально) Инварианты (что всегда должно быть верно для объекта) - invariants

Контрактный подход явно документирует код.

LSP требует, чтобы подклассы не нарушали "контракт" базового класса.
То есть:

Подклассы не должны ужесточать предусловия
```
class PaymentProcessor:
    def process(self, amount: float):
        assert amount > 0, "Amount must be positive"
        print(f"Processed payment: ${amount}")

class VIPPaymentProcessor(PaymentProcessor):
    def process(self, amount: float):
        assert amount >= 1000, "Minimum amount for VIP is $1000"  # ← ужесточение
        print(f"VIP processed: ${amount}")
```


Подклассы не должны ослаблять постусловия
📌 Определение:
Подкласс гарантирует меньше, чем базовый класс.
→ Это ломает ожидания клиента, который полагался на результат.
```
class Formatter:
    def format(self, text: str) -> str:
        result = text.strip()
        assert result != "", "Formatted text must not be empty"  # постусловие
        return result

class EmptyFormatter(Formatter):
    def format(self, text: str) -> str:
        return ""  # ← ослабленное постусловие: пустой результат
```
⚠️ Что происходит:
Базовый класс гарантирует непустую строку

Подкласс может вернуть пустую строку

🔥 Нарушение:
Клиентский код, ожидающий, что format() всегда возвращает непустой текст, может сломаться при подстановке EmptyFormatter.


Это ключевой принцип контрактного программирования, и он встроен в LSP.


✅ Как правильно
Подкласс может ослаблять предусловия (принимать больше допустимых входов)

Подкласс должен сохранять или усиливать постусловия (давать не меньше гарантии, чем базовый)



I. ASSERT
assert — это встроенная конструкция языка Python, используемая для проверки условий, которые должны быть истинны.
Если условие ложно — вызывается AssertionError.
assert условие, "Сообщение об ошибке"
```
x = 10
assert x > 0, "x должен быть положительным"
```

📌 Где используется:
- Проверка предусловий, постусловий, инвариантов
- Помощь в отладке: указывает на нарушение логики
- Как бюджетная альтернатива unit-тестам на ранних этапах

Если Python запущен с флагом -O (optimize), assert отключается — это значит, что не стоит использовать его для проверки пользовательского ввода или безопасности.

```
python -O script.py  # отключает все assert
```

II. ICONTRACT
icontract — это внешняя библиотека для контрактного программирования в Python.
Она позволяет явно задавать:

@require(...) — предусловия

@ensure(...) — постусловия

@invariant(...) — инварианты

```
import icontract

class Account:
    def __init__(self, balance: float):
        self.balance = balance

    @icontract.require(lambda amount: amount > 0, "Amount must be positive")
    @icontract.ensure(lambda self: self.balance >= 0, "Balance must be non-negative")
    def deposit(self, amount: float):
        self.balance += amount
```

Проблема хрупкости базового класса (fragile base class problem) — это проблема в объектно-ориентированном программировании, которая возникает, когда изменения в базовом (родительском) классе могут непредсказуемо сломать работу производных (дочерних) классов, даже если эти изменения кажутся безопасными и не нарушают сам базовый класс.
Причины возникновения:
- Наследование реализации, а не только интерфейса. Подклассы наследуют не только поведение, но и структуру родительского класса, включая внутреннюю реализацию.
- Переопределение методов. Подклассы могут переопределять методы, вызываемые внутри базового класса, что делает поведение системы менее предсказуемым.
- Сильная связанность. Подклассы зависят от деталей реализации родителя, даже если это не явно.
Последствия:
Ломаются подклассы при изменении родительского класса.
Трудности в сопровождении и расширении кода.
Уязвимости и баги, которые трудно диагностировать.
Способы борьбы:
- Соблюдение принципа Liskov Substitution (LSP).
- Композиция вместо наследования, если возможно.
- Использование абстрактных базовых классов, не содержащих реализации.
- Минимизация области видимости и использование private/protected.
- Финализация классов и методов (например, final в Java, sealed в C#).

Композиция и агрегация — это способы построения отношений между объектами в объектно-ориентированном программировании (в том числе в Python), альтернативные наследованию. Они позволяют создавать гибкие и легко сопровождаемые системы.

1. Наследование
Наследование — это механизм, при котором один класс (дочерний) получает свойства и поведение другого класса (родительского).
Минусы: жёсткая связанность, хрупкость базового класса.
2. Композиция
Композиция — это включение одного объекта внутрь другого, как части его реализации. Один объект владеет другим и управляет его жизненным циклом.
Ключевой признак: объект создаёт и использует другой объект, он полностью им владеет.
3. Агрегация
Агрегация — "ослабленная" композиция. Объект содержит ссылку на другой объект, но не управляет его жизненным циклом (тот может использоваться другими объектами).
Ключевой признак: зависимость внедряется снаружи (через параметры конструктора), объект не отвечает за создание/удаление зависимого объекта.


Виды связей между сущностями:

1. Композиция
Тип связи: Владение (ownership)
Один объект полностью владеет другим, отвечает за его создание и уничтожение.
Жизненный цикл вложенного объекта зависит от "владельца".

2. Агрегация
Тип связи: Ассоциация (слабое владение) или просто агрегация
Это "имеет, но не владеет".
Объект ссылается на другой, но не управляет его жизненным циклом.
Такая связь часто называется "агрегирующая ассоциация".

3. Наследование
Тип связи: Обобщение / Специализация
Дочерний класс — это специализация родительского,
Родительский класс — это обобщение дочернего.
Такая связь называется "is-a" (является).